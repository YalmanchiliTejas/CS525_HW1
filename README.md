# Code structure and run instructions

## Question 1:

    -> The first segment is in question1_1.c file and to run it run the run1.1 executable
    -> The second segment is in question1_2.c file and to run it, run the run1.2 executable

## Question 2:

    -> The first segment code is in the main.cpp file and to run it, run the ./run executable
    -> The second segment of the code is in the worker.cpp file and to run it, run the ./worker executable

## Question 3:

    -> The first segment code is in the file question3_1.c , to run it, run the executable run3.1
    -> The second segment code is in the file questison3_2.c, to run it, run the executable run3.2

## Question 4:

    -> The first segment code for loop i-j is in the file q4_norm.c, to run it, run the executable run_norm.
    -> The second segment code for loop j-i is in the file q4_interchanged.c, to run it, run the executable run_ic.
    -> NOTE: Value of n can be updated in each file.

## Question 5:

    -> The first segment code is in the file question5_1.c,  to run it, run the executable run5.1
    -> The second segment code is in the file question5_2.c, to run it, run the executable run5.2
    -> The third segment code is in the file question5_3.c, to run it, run the executable run5.3

## Question 6:

    -> The first segment code is in the file seg1.cpp, to run it, run the executable run1
    -> The second segment code is in the file seg2.cpp, to run it, run the executable run2

## Question 7:

    -> The first segment code is in the file question7_1.c, to run it, run the executable run7.1
    -> The second segment code is in the file question7_2.c, to run it, run the executable run7.2
